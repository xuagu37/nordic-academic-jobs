# University West
<p style="font-size: 1.2em; font-weight: bold;">Total jobs: 3</p>


<div id="filters" class="job-filters">
  <div class="filter-group">
    <label for="filterType">Job Type</label>
    <select id="filterType">
      <option value="">Show All</option>
      <option value="PhD">PhD</option>
      <option value="Postdoc/Researcher">Postdoc/Researcher</option>
      <option value="Lecturer/Professor">Lecturer/Professor</option>
      <option value="Research Engineer">Research Engineer</option>
      <option value="Other">Other</option>
    </select>
  </div>

  <div class="filter-group filter-search">
    <label for="jobFilter">Search</label>
    <input type="text" id="jobFilter" placeholder="Search jobs...">
  </div>
</div>

<div id="jobList">
<div class="job" data-country="Sweden" data-university="University West" data-type="Other" data-deadline="2026-12-31" style="margin-bottom: 1.5em;">
<h3>Mentorer för studenter med funktionsnedsättning</h3>
<ul>
  <li><strong>Link:</strong> <a href="https://hv.varbi.com/se/what:job/jobID:891953/type:job/where:3/apply:1?token=7dc05aa195b3303b527e21ea0e4322b4">View job posting</a></li>
  <li><strong>University:</strong> University West</li>
  <li><strong>Country:</strong> Sweden</li>
  <li><strong>Type:</strong> Other</li>
  <li><strong>Deadline:</strong> 2026-12-31</li>
</ul>

</div>

<div class="job" data-country="Sweden" data-university="University West" data-type="Other" data-deadline="2026-12-31" style="margin-bottom: 1.5em;">
<h3>Anteckningshjälp för studenter med funktionsnedsättning</h3>
<ul>
  <li><strong>Link:</strong> <a href="https://hv.varbi.com/se/what:job/jobID:891954/type:job/where:3/apply:1?token=f27dc5268edc25a680259cc84990e5b4">View job posting</a></li>
  <li><strong>University:</strong> University West</li>
  <li><strong>Country:</strong> Sweden</li>
  <li><strong>Type:</strong> Other</li>
  <li><strong>Deadline:</strong> 2026-12-31</li>
</ul>

</div>

<div class="job" data-country="Sweden" data-university="University West" data-type="Other" data-deadline="2026-10-03" style="margin-bottom: 1.5em;">
<h3>Avdelningschef för avdelningen för omvårdnad grundnivå</h3>
<ul>
  <li><strong>Link:</strong> <a href="https://hv.varbi.com/se/what:job/jobID:965922/type:job/where:3/apply:1?token=96fb8b8cea013376580677beb5b866b8">View job posting</a></li>
  <li><strong>University:</strong> University West</li>
  <li><strong>Country:</strong> Sweden</li>
  <li><strong>Type:</strong> Other</li>
  <li><strong>Deadline:</strong> 2026-10-03</li>
</ul>
</div></div>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const typeSelect = document.getElementById('filterType');
  const textInput = document.getElementById('jobFilter');
  const jobBlocks = document.querySelectorAll('.job');

  // Save original HTML so we can restore before highlighting
  jobBlocks.forEach(job => {
    const titleEl = job.querySelector('h3');
    if (titleEl) {
        titleEl.dataset.originalText = titleEl.textContent;
    }
  });

  function escapeRegExp(string) {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  }

  function highlightText(job, query) {
    const titleEl = job.querySelector('h3');
    if (!titleEl) return;
    
    const original = titleEl.dataset.originalText || titleEl.textContent;
    titleEl.dataset.originalText = original;
    
    if (!query) {
        titleEl.textContent = original;
        return;
    }
    
    const safeQuery = escapeRegExp(query);
    const regex = new RegExp(`(${safeQuery})`, 'gi');
    
    titleEl.innerHTML = original.replace(regex, '<mark>$1</mark>');
  }

  function updateDisplay() {
    const selectedType = typeSelect.value.toLowerCase();
    const query = textInput.value.trim().toLowerCase();

    jobBlocks.forEach(job => {
        
      const jobType = (job.dataset.type || "").toLowerCase();
      const titleEl = job.querySelector('h3');
      const titleText = titleEl ? titleEl.textContent.toLowerCase() : "";

      const matchesType = !selectedType || jobType === selectedType;
      const matchesQuery = !query || titleText.includes(query);

      if (matchesType && matchesQuery) {
        job.style.display = '';
        highlightText(job, query);
      } else {
        job.style.display = 'none';
      }
    });
  }

  typeSelect.addEventListener('change', updateDisplay);
  textInput.addEventListener('input', updateDisplay);
  updateDisplay();
});
</script>
